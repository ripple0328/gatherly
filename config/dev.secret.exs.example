import Config

# Copy this file to dev.secret.exs and update with your actual values
# dev.secret.exs is ignored by git and contains your local development secrets

# Phoenix endpoint secrets
config :gatherly, GatherlyWeb.Endpoint,
  # Generate a new secret key base with: mix phx.gen.secret
  secret_key_base: "CHANGE_ME_GENERATE_WITH_MIX_PHX_GEN_SECRET"

# Google OAuth credentials (get from Google Cloud Console)
config :gatherly, :google,
  client_id: "",
  client_secret: ""


# Authentication token signing secret (generate with: mix phx.gen.secret)
config :ash_authentication,
  token_signing_secret: "CHANGE_ME_GENERATE_WITH_MIX_PHX_GEN_SECRET"

# Optional: External API keys for third-party services
# config :gatherly, :external_api_key, "CHANGE_ME_YOUR_EXTERNAL_API_KEY"
# config :gatherly, :another_service_key, "CHANGE_ME_ANOTHER_SERVICE_KEY"
